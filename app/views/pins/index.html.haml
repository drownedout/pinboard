.container
	#pins.transition-enabled
		- @pins.each do |pin|
			.box.panel.panel-default
				.panel-bodu.text-center
					%h2= link_to pin.title, pin
					= link_to (image_tag pin.image.url(:medium)), pin
					%p Submitted by
					= pin.user.email